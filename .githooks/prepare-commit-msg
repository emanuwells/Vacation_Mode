#!/bin/sh
# Remove atribuição automática do Cursor Agent dos trailers de commit.
if [ -f "$1" ]; then
  sed -i.bak '/cursoragent@cursor\.com/d' "$1" 2>/dev/null || \
    sed -i '' '/cursoragent@cursor\.com/d' "$1" 2>/dev/null
  rm -f "$1.bak"
fi
